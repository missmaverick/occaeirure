function checkConditions(L, code) {
    if (L < 8 || parseInt(code, 10) === 0) {
        return false;  // Exit early if conditions are met
    } else {
        // Continue with other code if conditions are not met
        // ...
        return true;  // Return true or perform other actions
    }
}
